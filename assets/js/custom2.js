$(document).ready(function () {    let tableActionTO = null;    $('#signup-next').removeAttr('disabled');    $('#signup-next').click(function (e) {        e.preventDefault();        $(this).attr('disabled', 'disabled');        $('#signup-first').fadeOut('fast', function () {            $('#signup-second').fadeIn('fast');        });    });    $('.db-table-link-row').click(function () {        window.location = $(this).data('href');    });    $('.db-table-link-row').mouseenter(function () {        clearTimeout(tableActionTO);        let topOffset = $(this).offset().top - $(window).scrollTop() - 15;        let id = $(this).data('patient-id');        $('#table-action').css('top', topOffset + $(window).scrollTop());        $('#table-action').data('patient-id', id);    });    $('.db-table-link-row').mouseleave(function () {        tableActionTO = setTimeout(function () {            if (!$('#table-action').is(':hover')) {                $('#table-action').css('top', '-100px');                $('#table-action').data('patient-id', '0');            }        }, 500);    });    $('#delete-patient-trigger').click(function () {        let id = $(this).parent().data('patient-id');        let row = $('tr[data-patient-id="' + id + '"]');        let children = row.children();        let name = children[0].textContent;        let dob = children[1].textContent;        let ohip = children[2].textContent;        $('#del-name').html('<strong>' + name + '</strong>');        $('#del-dob').html('<strong>' + dob + '</strong>');        $('#del-ohip').html('<strong>' + ohip + '</strong>');        $('#delete-patient-btn').data('patient-id', id);        $('#delete-patient-modal').modal('show');    });    $('#edit-patient-btn').click(function () {        let id = $(this).parent().data('patient-id');        let row = $('tr[data-patient-id="' + id + '"]');        let children = row.children();        let name = children[0].textContent;        let dob = children[1].textContent;        let ohip = children[2].textContent;    });    $('#table-action').hover(function () {        clearTimeout(tableActionTO);        let id = $(this).data('patient-id');        let row = $('tr[data-patient-id="' + id + '"]');        row.addClass('hover');    },            function () {                let id = $(this).data('patient-id');                let row = $('tr[data-patient-id="' + id + '"]');                row.removeClass('hover');                tableActionTO = setTimeout(function () {                    $('#table-action').css('top', '-2000px');                    $('#table-action').data('patient-id', '0');                }, 500);            });    $(window).scroll(function () {        handleScroll();    });    $('.db-sidebar-toggle').click(function () {        $('.db-sidebar').toggleClass('db-collapsed');        $('.db-nav').toggleClass('db-sidebar-collapsed-pad');        $('.db-content').toggleClass('db-sidebar-collapsed-pad');    });    $('[data-toggle="tooltip"]').tooltip();    function handleScroll() {        if ($(window).scrollTop() > 0) {            $('.db-sidebar-hero').addClass('scrolled');            $('.db-nav').addClass('scrolled');        } else {            $('.db-sidebar-hero').removeClass('scrolled');            $('.db-nav').removeClass('scrolled');        }    }//    if ($(window).width() < 767) {//        $('.db-sidebar').addClass('db-collapsed');//        $('.db-nav').addClass('db-sidebar-collapsed-pad');//        $('.db-content').addClass('db-sidebar-collapsed-pad');//    }    $(window).bind("resize", function () {        console.log($(this).width())        if ($(this).width() < 767) {            $('.db-sidebar').addClass('db-collapsed');            $('.db-nav').addClass('db-sidebar-collapsed-pad');            $('.db-content').addClass('db-sidebar-collapsed-pad');        } else {            $('.db-sidebar').removeClass('db-collapsed');            $('.db-nav').removeClass('db-sidebar-collapsed-pad');            $('.db-content').removeClass('db-sidebar-collapsed-pad');        }    }).trigger('resize');    $(document).on('change', '.div-toggle', function () {        var target = $(this).data('target');        var show = $("option:selected", this).data('show');        $(target).children().addClass('hide');        $(show).removeClass('hide');    });    $(document).ready(function () {        $('.div-toggle').trigger('change');    });    var submit = $('input[type="submit"]');    var tabs = $('#tabs');    var parkList = $('#parkList');    submit.click(function (e) {        tabs.toggle();        parkList.toggle();    });});